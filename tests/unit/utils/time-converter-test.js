import timeConverter from 'dog-training-tools/utils/time-converter';
import { module, test } from 'qunit';

module('Unit | Utility | time-converter', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = timeConverter();
    assert.ok(result);
  });
});
